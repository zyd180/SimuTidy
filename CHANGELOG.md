# Changelog

本文件记录 SK2 的所有版本更新。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [3.4.1] - 2026-09-10

### 新增功能

- **拆分 Goto/From 的 Tag 命名可读化**（用户反馈）：Tag 不再加 3 位
  随机数（原 `GainA_047` 不可读），默认直接用清洗后的源信号名（即
  `GainA`）；撞当前层已有 Tag 时追加 `_1/_2` 确定性序号。随机数的
  初衷是防同名撞车（同 Tag 多 Goto 会让 From 连错、编译报错），查重
  后缀同样不撞且可读；查重范围限当前层（TagVisibility 默认 local 仅
  本层可见，极端跨层撞名场景留给编译诊断兜底）。测试：
  `testSplitGotoTagNameCollision`（预置同 Tag Goto → 新建为 `GainA_1`）
- **拆分 Goto/From 支持分支线**（用户需求，原 §12.2 已知限制废除）：
  一条线分出多个目标时，Goto 只建 1 个（源端），每个可行分支各建 1 个
  From（同 GotoTag，多 From 合法）；空间不足的分支**保留原连线不动**
  （部分拆分计入失败侧说明），全部分支放不下才整线跳过。核心依据
  （实验确认）：`delete_line(源端口, 目标端口)` 按端口对只删单条分支，
  其余分支保留。实现细节：
  - 选中线按**源端口分组并 union 同源全部记录的目标**：分支线在
    find_system 中返回主/支多条 line 记录（同一物理线），且记录结构因
    模型而异——新模型常合并为"1 条主线带 N 个 dst"，老模型可能存为
    "同源的多条独立记录、每条 1 dst"（用户实测复现：只按单条记录拆
    会漏掉其余分支，表现为"只有第一条被替换"）。Points 取 dst 最多
    的记录（主线，路径点最完整）
  - 各分支端点 y 取**目标端口坐标**（分支线 Points 只含主线路径）；
    From 碰撞排除本分支两端块及已可行的其他 From（分支相邻时 From
    可能互相压叠）
  - 推块/走廊逻辑保持只服务 Goto 侧，From 逐分支独立预算
  - 行为收紧（有意变更）：无推块路径的 From 位置现在也过碰撞检查
    （原先静默允许压叠）
  - 测试：`testSplitGotoFromMultiBranch`（1 源 3 目标 → Goto ×1 +
    From ×3、Tag 一致、各目标输入完好、线数 4）
- **GUI 冒烟测试**（新增 `tests/SimuTidyGUISmoke.m`，2 用例）：裁切
  bug 连续三版出现在主窗口而回归套件对 GUI 零覆盖。断言口径刻意
  收窄（防误伤）：
  - 只断言**分配格尺寸算术一致性**（grid 行高合计 vs 窗口高度）与
    控件齐全性，不猜渲染像素——uifigure 布局异步，程序化读 Position
    判断"被裁"不可靠（3.3.1 教训）
  - uifigure 创建失败走 **assume 跳过**（无头/旧版兼容，不挂部署门禁）
  - 清理是生死线：主窗口 timer 必须 stop/delete，否则 -batch 进程不退
    （closeAllTidyWindows 统一兜底）
  - 实测踩坑记录：面板标题带装饰空格须 strtrim 比对；cellfun 不能
    直接用于图形对象数组（用 arrayfun）；`SimuTidy_nameDialog` 无输出
    参数（经 findall 定位单例）；**findall(fig对象) 不遍历 uifigure
    子组件**（实测返回 0 后代，须 findall(0,...) 全局找再按所属窗口
    过滤）；部分 MATLAB 会话里 uifigure 创建后 ~1s **自动关闭**
    （裸窗口亦然，自动化/无头渲染环境特性）——`assumeStable` 探测后
    按假设跳过，真实桌面环境正常执行

### 修复

- **主窗口/对话框单例返回死窗口**（"关窗立刻重开"必现）：uifigure 的
  `delete` 是**异步**的——刚关闭的窗口短暂残留在 findall 结果里且
  `isvalid` 仍为 true（异步中间态），单例检查直接把它返回给用户。
  isvalid 过滤治不了根（实测），改为**存活标记**方案：创建时打
  `SimuTidy_Alive`=true，关闭路径（onClose/onToggleTheme/对话框
  CloseRequestFcn/关闭按钮）先摘标再 delete，单例只认有标记的窗口。
  涉及 SimuTidy_mainGUI / SimuTidy_nameDialog / SimuTidy_onboarding
- **测试套件移植性**：`SimuTidyRegression` 的根目录原为 Constant 硬编码
  `'F:\OpenCode\SimuTidy'`——clone 到其他路径测试即挂（公开仓库后他人
  无法跑部署门禁）。改为 TestClassSetup 内从本文件位置动态解析。
  踩坑记录（写给后续改测试的人）：① Constant 属性必须是常量表达式，
  不能在声明处调函数；② `mfilename` 在 classdef 方法内返回**空串**，
  需用 `which('SimuTidyRegression')` 定位文件再 fileparts 两次上溯到
  项目根；③ 坑②曾靠"当前目录恰好在项目内"掩盖，验证移植性必须把
  当前目录切到项目外再跑
- **benchmark.m 残留清理**：addpath 列表还留着 3.1.0 已并入包的
  `core` 目录，删除

## [3.4.0] - 2026-09-10

## [3.4.0] - 2026-09-10

### 新增功能

- **主窗口"运行日志"区**（用户提议）：检查与诊断下方新增只读日志区，
  自动记录每次操作完整明细（对齐基准/跳过原因/诊断发现，带
  `[INFO]/[WARN]` 前缀）——此前这些只在 MATLAB 命令行可见，GUI 侧
  状态栏只能看到最后一条。设计取舍：
  - `internal.log` sink 转发范围扩为 **info 及以上**（原 warn/error），
    `testLogSink` 同步更新
  - **新行插顶部**：uitextarea 无自动滚动 API，追加到底部最新内容
    不可见；插顶保证最新明细始终可见（时间倒序，为可见性取舍）
  - 上限 200 行防无限增长；命令行输出不变；状态栏保留（负责"当前
    结果+颜色语义"，日志区负责"历史明细"）
  - 标题"运行日志"用 uipanel Title 固定在日志区上方（首版混在
    textarea 首行，新行插顶后被推下去，用户反馈改面板标题）
  - 窗口默认/最小高度 700→800（行高合计 ~760，同 3.3.1 留余量经验）
  - 新手引导提示补充日志区说明

- **检查与诊断三件套**（用户选定 1+4+2 方案，统一走"检查 → res → 结果面板逐条定位"模式）：
  - **高亮未连接端口升级**：`highlightUnconnected` 新增可选 res 输出，
    发现项逐条**分类**（N 个输入/输出/控制端口未连接、悬空信号线）并带
    可定位句柄；GUI 有发现时弹结果面板逐条"定位"（命令行行为不变）；
    顺带收敛悬空线检测的两处重复 try/catch
  - **模块重叠检测**（新增 `simutidy.checkOverlaps` / `slCheckOverlaps`）：
    当前层全块两两 AABB 检测（O(n²) 纯数值，实测毫秒级），发现项入
    结果面板；复用与 `collidesAny` 同口径的相交判定
  - **Goto/From 配对诊断**（新增 `simutidy.checkGotoFrom` /
    `slCheckGotoFrom`）：检查悬空 Goto（全模型无 From 引用）、无源 From、
    **跨层 local 标签引用**（local 仅本层可见，仿真报错）；刻意不做编译
    校验，只覆盖确定性问题（同 Tag 多 Goto 等复杂语义留给仿真诊断）
  - 结果面板新增可选 `res.okLabel`/`failLabel` 文案钩子（诊断类无
    "失败"概念，显示"连接完好/有问题"等；缺省回落"成功/失败"兼容旧 res）
  - GUI"检查与诊断"区改为三按钮并排（不增行高，主窗口总高不变）；
    Tools 菜单与 Toolstrip 选项卡同步新增两项；新绘专属图标
    `checkoverlap`（前后交叠方块）与 `checkgoto`（双标签块+箭头，
    浅/深两套，程序化绘制进 `SimuTidy_makeIcons`），初版曾临时复用
    highlight 图标
- **Toolstrip 选项卡分区与主窗口对齐**（用户反馈）：原 2.7.0 布局只有
  4 个分区（工具/模块整理/连线整理/接口与诊断）且"导出 Web 视图"
  从未上过选项卡。重排为与 GUI 一致的 6 分区：工具 → 视图与导出
  （新增 exportAction，配新绘 `export` 图标）→ 模块整理 → 连线整理 →
  接口与命名 → 检查与诊断（三诊断项归位本分区）
- 测试同步：新增 `testHighlightResClassification` / `testCheckOverlaps` /
  `testCheckGotoFrom` 三个用例（共 25 用例）

### 修复

- **高亮未连接端口 GUI 路径报错**（用户点击暴露，两处）：
  - `highlightUnconnected` 以 `([], 'Progress', fig)` 三参调用时，
    `if nargin < 2 || isempty(clearFlag)` 左边为 false、右边读取未定义
    的 clearFlag → 抛"函数或变量 'clearFlag' 无法识别"。3.3.0 加
    Progress 选项引入的存量 bug（回归测试只覆盖 1/2 参调用未踩到）。
    修复：clearFlag 默认值先于条件分支赋值
  - 修复上条后多输入块又触发新错：`countUnconnected` 对多句柄
    `get_param(ports, 'Line')` 直接 `== -1`——多句柄返回 **cell**
    （与 Position 同款多值行为，`batchPositions` 归一过的同一坑），
    抛"'cell' 类型的函数 'eq' 未定义"。修复：iscell → cell2mat 归一
  - 进度对话框加 `onCleanup` 兜底关闭——中途报错时 uiprogressdlg
    不再模态挂住主窗口（用户此前"卡死"的直接观感来源）
  - 测试补 `testHighlightWithProgressOption`（Progress 路径 + 多输入
    Logic 块 + 清除模式共存），此前只测过 1/2 参调用
- **模块对齐/大小统一/连线端口对齐非预期行为的两处根因**（用户报告"有时出现非预期对齐"）：
  - **等间距破坏**：`alignBlocks` 的 hspace/vspace 原实现跳过基准块——
    等间距锚点是"中心最小/最大"两块，基准块（最上最左）在中间时被
    跳过导致等距失效（实测 4 块间距 250/185/215），基准是唯一中间块
    时整体空转且日志虚报调整数。修正：基准块正常参与分布；日志改报
    实际调整数并改用锚点话术（等间距模式不适用"基准不动"承诺）
  - **跨层选中混算**：三个功能的选中查询均不限层，父层与打开的
    子系统窗口的选中块被混入同一坐标系计算，产生非预期大位移/改尺寸
    （实测复现）。修正：`alignBlocks`/`uniformSize`/`alignLinePorts`
    的选中查询统一加 `SearchDepth=1`，只处理当前层（与
    `highlightUnconnected` 3.1.0 限层修正同一理由）
- **基准提示增强**（用户反馈易搞不清基准是谁）：对齐/大小统一的命令行
  日志明示"基准模块：<名字>（所选块中最上最左）"；GUI 等间距按钮
  tooltip 注明锚点规则；新手引导使用提示补充基准与作用范围说明

### 变更

- **清除信号线名功能细化**（用户需求）：信号线命名对话框重排，
  "清除所有"上方新增**"清除所选信号线命名"**按钮——只清选中的线，
  未选中任何线时不做处理、仅 WARN 提醒（不回落全线清除）；"清除所有
  信号线命名"改为弹窗二次确认（默认取消），确认后无视选中状态清当前
  层级全部线（新 `clear_all` 模式，与既有 `clear` 的"选中优先"语义区分，
  `clear` 行为不变）。配套：
  - `simutidy.autoNameSignals` 新增 `clear_sel`/`clear_all` 两模式
  - 新增命令行入口 `slClearSignalNames`（转 `clear_sel`）
  - 对话框加高 50px（`cfg.gui.nameDialogPos` 350），嵌套
    `sltidy_iif` 文案改 switch
- 测试同步：新增 `testClearSelectedAndAllModes` 锁定三场景（选中清除/
  零选中提醒/全部清除）
- **生成接口命名规则**（用户需求）：`simutidy.generatePorts` 新生成的
  Inport/Outport 块名改为优先取子系统**内部对应端口块**（按端口号匹配）
  的名称（内部叫 Vin，父层生成块也叫 Vin），原固定 `InportN/OutportN`
  序号命名（含跳号怪癖）废除，仅作为内部块缺失时的回落兜底保留。
  细节：
  - 内部名经 `internal.sanitizeName` 清洗非法字符
  - 父层重名追加 `_1/_2` 序号（与 `updateBlockNames.renameBlock` 同策略；
    原版"撞名即静默跳过不生成"的守卫语义随之删除）
  - 连线改用**端口句柄** `add_line`（同 `splitGotoFrom` 3.1.0 的修复
    原因：块名含 `/` 时路径拼接必然断线，句柄不受名称影响）
  - 汇总日志追加本次生成的块名清单，便于核对命名结果
  - **默认显示模块名称**：新生成块 ShowName='on' + IconDisplay='Port
    number'（与 `updateBlockNames` 显示约定一致；原 'Signal name' 图标
    模式在信号线未命名时图标空白，模块名不突出）
  - **生成后的 update 受 `cfg.simulink.updateAfterChange` 门控**（与
    `setSignalResolve` 同款写法）：该 update 只为立即刷新新端口的显示，
    触发整模型编译在大模型上秒级、非正确性必需；默认仍开启（行为不变），
    关闭开关即可跳过编译提速
- 测试同步：`testGeneratePorts` 改锁新命名规则（Vin/Vout），新增
  `testGeneratePortsConflictAndFallback` 覆盖"清洗+冲突后缀"路径

---

## [3.3.1] - 2026-09-09

### 修复

- **主窗口布局裁切**（用户截图反馈）：高 DPI 下两处控件被压扁——
  ① 标题行 26px 裁掉 15pt 文字与按钮 → 行高加至 34；
  ② "连线整理"第二行嵌套 grid 被外层固定行高挤成细条 → 拍平为单层
  2 行网格（跨列布局），全表行高加余量、各分区 RowSpacing 显式置 8
- **顶部/底部按钮下边缘被裁**：嵌套 grid（顶部标题行、底部"设置"行）
  未显式设 Padding——uigridlayout 默认 8px 内边距把固定行高吃掉 16px，
  内部按钮画不下被裁。两处均显式 Padding=0 + 行高与所在行一致
- **旧窗口尺寸偏好裁底**：gridlayout 固定行高合计 ~660px，默认/记忆高度
  620 会裁掉底部分区 → 默认窗口加高至 700，记忆值下限同步抬到 700
  （旧偏好自动被抬升，无需用户清理）
- **测试不再写真实用户配置**：`testUserConfigOverride` 曾直接在
  userpath 上做备份/还原，文件锁导致坏 JSON 残留、用户每次启动报警告
  → `userConfig('path')` 新增 `SIMUTIDY_USERCONFIG` 环境变量覆盖，
  测试重定向到临时目录，备份/还原机制整体删除
- 经验沉淀（写给后续改布局的人）：uifigure 高 DPI 下控件最小尺寸变大，
  gridlayout 固定行高必须留余量；**嵌套 grid 必须显式 Padding 与
  RowHeight**（默认内边距会吃外层空间，导致内部控件被裁）；程序化读取
  按钮 Position 判断"是否被裁"不可靠——布局异步完成读到的可能是未
  布局值，且 Position 反映的是分配格尺寸而非实际绘制裁切，最终以用户
  截图为准

---

## [3.3.0] - 2026-09-09

### 新增功能（对应原 3.0.0 规划"第一批（高可行高价值，先行交付）"，连线样式着色经评估移除）

- **统一日志系统（薄实现）**
  - `simutidy.internal.log`：debug/info/warn/error 四级，`cfg.log.level` 门控（默认 info）
  - 命令行输出带 `[INFO]/[WARN]/[ERROR]` 前缀（正文文案不变）；GUI 启动注册状态栏
    sink，核心函数内部的 WARN/ERROR 自动上屏（琥珀/红），关窗自动注销
  - 刻意不做：落盘、多 sink、等级切换 UI（工具规模用不上，需要时接口不变地扩展）
- **用户级配置文件**
  - `SimuTidy_config()` 支持 `userpath/SimuTidy_config_user.json` 覆盖默认值
    （优先级：代码默认 < 用户 JSON < 主题偏好）
  - 白名单+类型双重校验与写入端共用一张表（`+internal/userConfig`）；坏文件只
    一次性告警并回落默认，永不让工具启动失败
  - JSON 用**嵌套对象**格式——平铺点键（"goto.gap"）会被 jsondecode 改写为
    合法标识符无法往返（踩坑定稿，写进实现注释）
- **操作结果反馈面板**：拆分/端口对齐/命名三个批量操作新增**可选输出 res**
  （op/okCount/failCount/failItems，含可定位句柄）；GUI 在有失败时弹非模态
  "操作结果"面板，逐条失败原因 + **一键定位**（打开所在系统并红色高亮）；
  命令行不接输出行为不变，slXxx 包装零改动
- **主窗口可缩放布局**：绝对定位重构为 uigridlayout（5 分区 9 行网格），
  窗口可拖拽缩放、控件随格伸展；**记住窗口位置与尺寸**（关窗/切主题时写偏好，
  重开恢复，越界自动夹回屏幕）
- **操作进度条**：拆分/端口对齐/高亮/改名四个批量函数支持 `'Progress', <窗口句柄>`
  选项——仅 GUI 传句柄时弹 uiprogressdlg（含取消，取消后按已完成量正常汇报）；
  **命令行/脚本永不弹窗**（不传即零开销）
- **设置界面**：主窗口右下"设置"按钮 → 对话框编辑 Goto 尺寸/间距、命名最大
  长度、刷新间隔、updateAfterChange、主题；保存经白名单校验写入用户 JSON。
  刻意不暴露 replaceChars 正则等高级项（留 JSON 手编）

### 变更

- **部署门禁**：`SimuTidy_deploy.bat` 先跑回归测试，任一用例失败即中止安装
- 测试套件扩至 20 用例（新增：用户配置合并/坏文件回落、日志 sink 转发与注销、
  res 结构与失败对象句柄）

### 修复

- 批量读取 Position 在**单块子系统**下崩溃（get_param 单元素返回标量而非 cell）：
  抽取 `+internal/batchPositions` 统一归一，对齐/大小统一/端口对齐/拆分 4 处接入

### 调整记录

- **移除"连线样式着色"**（原第一批条目）：评估后价值低于成本——Simulink 线型
  表达力弱、批量设色场景少，且本批其余 7 项已形成完整体验闭环；后续有需求
  按 res 面板同款模式补（约半天）

---

## [3.2.0] - 2026-09-09

### 新增功能（对应原 3.0.0 规划"3. UI 界面美化，提升使用体验"）

- **GUI 深色主题**
  - `SimuTidy_config` 改为双调色板结构 `cfg.themes.light/dark`（字段名两套完全一致），
    运行时按用户偏好解析为 `cfg.colors`——消费方零改动换肤（2.6.1"颜色全集中"设计的直接收益）
  - 主窗口标题栏新增**主题切换按钮**（"深色主题/浅色主题"，与"使用引导"并排）；
    切换写偏好 `setpref('SimuTidy','theme',...)` 并重建窗口（复用既有定时器清理与单例逻辑）
  - 偏好持久化，非法/缺失值一律回落浅色，零迁移成本
  - 深色调色板：背景 #1E2227 系深灰蓝，分区色整体提亮一档保持"分区即配色"可辨识度，
    文字全反转，成功/错误色提高明度保对比度
- **Toolstrip 图标深色变体（最小交付）**：`SimuTidy_makeIcons('dark')` 生成亮色变体至
  `resources/icons_dark/`（同文件名，不动现有 icons/）。按规划"视需求另行评估"未做
  自动切换；需要时改 `simutidyTab_actions.json` 的 `icons/` 路径前缀为 `icons_dark/`
  并执行 `slReloadToolstripConfig` 即可

### 修复

- **工具条图标自 2.7.0 起一直近乎全黑（存量 bug，本次做深色变体时发现）**：
  调色板 0~1 浮点色直接赋给 uint8 画布被截断为 0（`uint8(0.169)=0`），所有图标
  实际渲染为近黑色。已按 255 缩放取整后进画布，浅色图标恢复分区色显示
- **GUI 硬编码色清零**：主窗口/命名对话框/新手引导中约 10 处写死的灰/白色
  （标题、模型标签、状态栏、开发者信息、灰按钮等）全部收敛为 `cfg.colors`
  新字段（text/textSubtle/textFaint/btnGrey），主题化前置条件达成

---

## [3.1.0] - 2026-09-09

### 新增功能（对应原 3.0.0 规划"1. 重新设计项目架构，优化性能与工程质量"）

- **架构重构（命名空间化）**
  - 核心功能由平铺的 `core/sl*.m` 迁入 `+simutidy` 命名空间包（`simutidy.alignBlocks` 等 10 个实体）
  - 根目录保留 10 个 `sl*.m` 一行薄包装，**签名/输出/报错完全不变**，Toolstrip JSON、
    sl_customization 菜单、用户既有脚本零改动
  - 新增 `+simutidy/+internal` 共享层：`resolveSystem`（收敛 7 处 sys 校验样板）、
    `collidesAny`（收敛 2 份重复碰撞实现）、`sanitizeName`（收敛 3 处重复清洗）、
    `report`（统一批量操作汇总格式）、`capabilities`（特性检测集中管理）
  - GUI 回调直调 `simutidy.*`，不再经兼容包装
- **兼容层抽象**
  - 特性检测（Toolstrip 支持/Report Generator 许可证）集中到 `simutidy.internal.capabilities`
  - `sl_customization.m` 改由 `resources/templates/` 模板生成（占位符仅 @VERSION@），
    替代原 ~170 行 fprintf 拼接，菜单改动不再需要改安装脚本

### 性能优化（N=500 块串联模型实测，选中不计入计时，3 次取最优）

| 操作 | 优化前 | 优化后 | 提速 |
|------|--------|--------|------|
| 连线端口对齐（单块） | 0.217s | 0.002s | >50× |
| 大小统一（全选 500 块） | 1.155s | 0.353s | ~3.3× |
| 模块对齐（全选 500 块） | 0.769s | 0.531s | ~1.45× |
| 信号命名（499 条线） | 0.476s | 0.388s | ~1.2× |
| 高亮未连接（豁免项） | 1.296s | 1.265s | — |

- **几何操作不再触发模型编译**（主收益）：对齐/大小统一/端口对齐移除了操作后的
  `SimulationCommand update`（单次整模型编译 500 块实测 ~0.16s）；Simulink 移动块后
  自动重排连线，编译刷新纯属浪费。`cfg.simulink.updateAfterChange` 语义相应收窄为
  "仅对需编译校验的操作生效"（信号对象解析、生成接口）
- **幂等跳过**：对齐/大小统一对"目标位置==现位置"的块跳过写入；端口对齐拉直时
  已平直的线跳过 Points 写入（带连线块的 set_param 实测 ~1.1ms/次，来自 Simulink
  逐块连线重排内部机制，公开 API 无法绕过——这是对齐类提速上限的根源）
- **批量读取**：位置读取由逐块 get_param 向量化为单次调用（对齐/大小统一/端口对齐缓存）
- **定向查找**：更新模块名称由"全块遍历+逐块取 BlockType"改为按 BlockType 定向查询
- **行为修正**：高亮未连接端口与连线端口对齐的扫描范围收敛为当前层
  （SearchDepth=1）。原实现递归全模型，把子层块混入当前层坐标系做碰撞/诊断
- **测量说明**：信号命名与模块对齐提速低于 3× 的原因——剩余耗时为 set_param
  写侧（线名/位置各不相同无法合并，且每次写入产生 undo 记录与连线重排），
  属 Simulink 内部机制，已列豁免

### 工程质量

- **版本控制**：项目初始化 git 仓库，重构全程按 Phase 提交，可回溯
- **回归测试**：`tests/SimuTidyRegression.m`，14 个用例锁定全部功能现有行为
  （含 2 个已知怪癖：生成接口跳号、改名与非 IO 块同名时静默失败），重构前后全绿
- **性能基准**：`tests/benchmark.m`，N 块模型自动计时，结果落盘 `tests/output/`
- **错误 ID**：核心错误补 ID（`SimuTidy:invalidSystem` 等），便于上层分类捕获
- **死代码清理**：删除无调用方的 `SimuTidy_alignDialog` 对话框及 `cfg.gui.alignDialogPos`
- **局部遮蔽修复**：splitGotoFrom 的 `dir` 变量改为 `moveDir`（不再遮蔽内置函数）

---

## [3.0.0] - 2026-09-08

### 变更

- **项目更名 SK2 → SimuTidy**（破坏性变更，已安装用户请先运行旧版 `SK2_uninstall` 再安装新版）
  - 入口/配置/安装/卸载/检查/版本/部署脚本及 gui、utils 全部文件与函数同步更名：
    `SK2.m`→`SimuTidy.m`、`SK2_config.m`→`SimuTidy_config.m`、`SK2_install.m`→`SimuTidy_install.m`、
    `SK2_uninstall.m`→`SimuTidy_uninstall.m`、`SK2_check.m`→`SimuTidy_check.m`、`SK2_version.m`→`SimuTidy_version.m`、
    `SK2_deploy.bat`→`SimuTidy_deploy.bat`、`gui/SK2_*.m`→`gui/SimuTidy_*.m`、`utils/SK2_makeIcons.m`→`utils/SimuTidy_makeIcons.m`、
    `utils/sk2_iif.m`→`utils/sltidy_iif.m`、`utils/sk2_getModelName.m`→`utils/sltidy_getModelName.m`
  - 新手引导偏好命名空间 `getpref/setpref('SK2',...)` → `('SimuTidy',...)`（老用户会重新看到一次引导）
  - Toolstrip 资源更名：`sk2Tab*.json`→`simutidyTab*.json`、组件名与选项卡标题 → SimuTidy、`openSK2_*` 图标 → `openTidy_*`
  - 生成的 `sl_customization.m` 菜单 tag 由 `SK2*` 改为 `SimuTidy*`；`SimuTidy_uninstall` 兼容识别旧版 SK2 残留
  - core 层 `sl*` 公共 API 命名不变，命令行调用方式不受影响（仅配置函数更名）

### 修复

- **零参调用报"输入参数的数目不足"**（3.1.0 开发期回归，用户报告后修复）：
  迁移 +simutidy 包时 `nargin<1 → sys=gcs` 守卫被误搬进 resolveSystem，
  但未传参时 sys 是未定义变量，作实参在调用点即抛错，走不到内部空值分支。
  GUI 按钮均显式传 `[]` 不受影响；Tools 菜单/Toolstrip/命令行的零参调用
  （拆分/高亮/解析/命名/对齐/大小统一 7 个功能）全部中招。
  已恢复入口守卫并新增 testZeroArgInvocations 回归用例
- **`SK2_check`（现 `SimuTidy_check`）无法运行**：子目录检查行字符串缺少引号导致整函数解析失败，已修复

### 移除

- **"打包子系统"功能**（`slCreateSubsystem`）：与 Simulink 原生框选 + Ctrl+G 完全等价，属冗余入口。GUI 按钮、Tools 菜单项、Toolstrip 按钮及 subsys 图标同步移除；打包请直接使用 Simulink 原生 Ctrl+G 或 `Simulink.BlockDiagram.createSubSystem`

---

## [2.8.0] - 2026-09-08

### 新增功能

- **信号对象解析批量勾选**（`slSetSignalResolve`）：为当前层级所有有名字的信号线批量勾选"信号名称必须解析为 Simulink 对象"
  - 作用范围当前层级（不影响子层级）；无名字的线自动跳过；幂等（已勾选不重复计数）
  - 支持 `'on'`（默认）/`'off'` 取消；选中线时仅处理选中的线
  - 勘选后更新图时 Simulink 将强制校验信号名指向基础工作区对象（如 Simulink.Signal）
  - 陖成 GUI"连线整理"分区按钮（含自定义图标）、Toolstrip"连线整理"Section 大按钮

---

## [2.7.1] - 2026-09-08

### 新增功能

- **新手引导（首次启动功能导览）**：首次打开主窗口自动弹出 3 步引导向导（欢迎 / 功能分区 / 使用提示），可勾选"不再自动显示"
  - 主窗口标题栏新增"使用引导"按钮，随时重新查看
  - `SK2_config` 新增 `cfg.gui.showOnboarding` 开关，可彻底关闭自动弹出

### 修复

- **单例检查失效**：uifigure 默认句柄隐藏（HandleVisibility='off'），`findobj` 单例检查全部失效导致重复开窗；主窗口/引导/对齐/命名对话框统一改用 `findall`

### 谘整

- 版本规划：将"新手引导"从 3.0.0 提前至 2.7.1 交付

---

## [2.7.0] - 2026-09-08

### 新增功能

- **Simulink Toolstrip「SK2」选项卡**：在顶部工具条（格式 与 APP 之间）新增 SK2 专属选项卡
  - 第一个按钮 = 打开 SK2 主窗口（大图标按钮）
  - 其余 10 个功能按 4 个分区排列：工具 / 模块整理 / 连线整理 / 接口与诊断，大按钮样式与 APP 栏一致
  - "模块对齐"为下拉按钮，收纳 8 种对齐与等间距分布
  - **自定义图标**：18 个扁平分区色图标（16/24 双尺寸、透明背景 PNG），由 `utils/SK2_makeIcons.m` 脚本生成，可修改后一键重建
  - 基于 `resources/` 组件文件夹 + 官方 Toolstrip 定制 API（约 R2022b+，旧版自动回退 Tools 菜单）
  - `SK2_install` 新增第 5 步自动注册；`resources/` 在 MATLAB path 上即自动加载
  - 原有 Tools→SK2 菜单保留

---

## [2.6.1] - 2026-09-08

### 改进

- **UI 配色重新设计（靛青专业系）**：低饱和分区配色，"分区即配色"
  - 视图与导出=深海蓝、模块整理=钢青蓝、连线整理=青绿、接口与命名=靛紫、检查与诊断=琥珀（警示语义）
  - 每分区主操作用深色变体（导出/大小统一/打包/端口对齐/更新模块名称）
  - 面板标题文字深灰、窗口背景浅灰白；状态栏成功绿/出错红
  - 全部颜色集中到 `SK2_config.m`（`cfg.colors.*`），GUI 不再硬编码 RGB；对齐/命名对话框同步换色
- **一键部署脚本**：新增根目录 `SK2_deploy.bat`，双击自动调用 MATLAB 静默完成安装（要求 R2019a+ 且 matlab 在 PATH 中）

---

## [2.6.0] - 2026-09-08

### 变更

- **"拉直选中连线"升级为"连线端口对齐"**（函数 `slStraightenLines` 更名为 `slAlignLinePorts`）
  - 新行为：以选中模块连线的另一端端口为基准（基准端不动），垂直移动选中的模块使其端口与基准端口同高，对齐后连线成为纯水平直线
  - 只调整选中模块的垂直位置（y），绝不改变水平位置（x）
  - 对齐依据线：优先用户同时选中的连线，否则取模块第一条已连接连线；同模块其余连线在模块一侧拉直
  - 移动前碰撞检查：垂直平移会与周围模块重叠时跳过该模块（不做任何修改）
  - 单个失败不影响其他，最后汇总"对齐 X 个，跳过 Y 个"
  - 已安装用户需重跑 `SK2_install` 刷新 Tools 菜单项

### 修复

- **多选模块碰撞误判**：碰撞检查改为两阶段裁决——先基于初始几何算出所有选中模块的目标位置，再在"全部移动后"的虚拟布局上逐个检查；此前会把尚未移动的其他选中模块的旧位置误判为障碍
- **连线拉直二次平移**：模块移动后 Simulink 会自动更新连线端点，旧逻辑再叠加一次位移导致中间点错位；改为移动后重读线点，将模块一侧及中间点压平到当前端口高度

---

## [2.5.1] - 2026-09-08

### 改进

- **主界面重新设计**：按实际功能分为 5 个分区（uipanel 分组）
  - 视图与导出：导出 Web 视图
  - 模块整理：对齐/等间距（8 种）、大小统一、打包子系统
  - 连线整理：拉直选中连线、信号线命名、拆分 Goto/From
  - 接口与命名：生成接口、更新模块名称
  - 检查与诊断：高亮未连接端口
  - 窗口加宽至 460×660，按钮改为统一网格布局；按钮回调、悬停提示、配色保持不变

---

## [2.5.0] - 2026-09-08

### 新增功能

- **Goto/From 批量拆分**：支持同时选中多条信号线批量拆分
  - 单条失败不影响其他连线，最后汇总"成功 X 条，失败 Y 条"及原因
  - Goto 在源块右侧、From 在目标块左侧，各留间距（`cfg.goto.gap`）
  - Goto/From 高度与该线所连 Inport/Outport 块高度一致，宽度用配置默认值
  - 空间不足时自动推开源块/目标块，推块前做碰撞检查，避免撞上周围模块
  - 实在放不下的连线安全跳过（不做任何修改），并在汇总中提示

---

## [2.4.0] - 2026-09-05

### 新增功能

- **更新模块名称**：沿信号链追溯源端名
  - Inport 命名：信号线名 → 外层传入名（父层端口连线名）→ 沿源端递归追溯
  - Outport 命名：信号线名 → 沿输入线源端递归追溯（源为子系统时取内部 Outport 块名）
  - 支持深层级模型（实测 5000+ 端口块全量改名）
  - 重复运行无变化（幂等）

### 改进

- **显示模块名称**：改名后设置 `IconDisplay='Port number'`，图标内显示端口号，模块名称外显
- **命名规则**：Inport/Outport 同层重名自动追加序号，跨层级可同名

---

## [2.3.0] - 2026-09-05

### 新增功能

- **更新模块名称**：更新 Inport/Outport 模块名称为信号名
  - Inport 取输出连线信号名，无信号名时用外层传入名（父层端口连线名）
  - Outport 取输入连线信号名，无信号名时保持原名
  - 同层 Inport/Outport 重名自动追加序号
  - 集成到 GUI 按钮和 Simulink Tools 菜单

---

## [2.2.0] - 2026-09-04

### 新增功能

- **生成接口**：自动为Subsystem未连接端口添加Inport/Outport并连线
  - Inport背景色：浅蓝[0.6, 0.85, 1]
  - Outport背景色：[1, 0.333, 1]
  - 支持三种显示模式：
    - `name` - 仅显示信号名称（默认）
    - `number` - 仅显示端口号
    - `both` - 显示端口号和信号名称
  - 自动检测端口类型和位置
  - 支持多个未连接端口批量处理

### 改进

- **高亮未连接端口**：每次运行自动刷新高亮状态，已连接端口自动取消高亮
- **移除无效分隔符**：修复sl_customization.m中`{[]}`导致的报错
- **安装脚本优化**：改用fprintf直接写入文件，避免字符串拼接过长导致崩溃

---

## [2.1.0] - 2026-09-04

### 新增功能

- **永久集成安装**：安装脚本自动配置永久路径和启动项
- **自动菜单刷新**：安装后自动刷新 Simulink 菜单，无需手动操作
- **卸载脚本**：新增 `SK2_uninstall.m`，一键移除 SK2
- **版本查询函数**：新增 `SK2_version.m`，方便查询当前版本
- **完整工具栏菜单**：包含所有功能的子菜单，结构更清晰

### 改进

- **安装脚本优化**：自动备份已有 `sl_customization.m`
- **README 更新**：添加永久集成安装说明
- **CHANGELOG 格式**：遵循 Keep a Changelog 规范

---

## [2.0.0] - 2026-09-04

### 架构重构

- **模块化重构**：将单文件（1242行）拆分为15个独立模块
- **GUI与逻辑分离**：GUI代码移至 `gui/` 目录，核心功能移至 `core/` 目录
- **配置集中管理**：新增 `SK2_config.m`，所有可调参数统一管理
- **工具函数独立**：通用工具函数移至 `utils/` 目录

### 新增功能

- **安装脚本**：新增 `SK2_install.m`，一键安装到 Simulink 工具栏
- **安装检查**：新增 `SK2_check.m`，排查安装问题
- **完整工具栏集成**：安装后在 Tools 菜单显示所有8个功能
- **子菜单支持**：模块对齐和信号线命名支持子菜单选择

### Bug 修复

- **定时器资源泄漏**：修复窗口关闭时 timer 对象未正确清理的问题
- **数组索引越界**：修复 `dstPortH(1)` 在空数组时的索引错误
- **拉直连线逻辑**：改为调整连线路径点，不再移动模块位置
- **命名冲突处理**：使用循环检查确保 Goto/From 名称唯一
- **userpath 空值显示**：修复 userpath 为空时的显示问题

### 代码优化

- **warning 函数格式**：修正为 `warning(id, msg)` 标准格式
- **datetime 替换**：`datestr(now)` 改为 `datetime('now')`
- **try-catch 格式**：统一为标准多行格式，移除行内写法
- **变量预分配**：优化循环中动态增长数组的性能

### 文档

- **README.md**：新增完整项目说明文档
- **CHANGELOG.md**：新增版本更新日志
- **代码注释**：所有公共函数添加完整的帮助文档

---

## [1.0.0] - 2026-08-10

### 初始版本

- **GUI 界面**：基于 App Designer 的图形用户界面
- **导出 Web 视图**：支持将模型导出为 HTML Web 视图 ZIP
- **拉直连线**：自动调整连线为水平或垂直
- **模块对齐**：支持8种对齐和分布方式
- **模块大小统一**：支持4种大小统一模式
- **信号线命名**：支持3种命名方式和清除功能
- **Goto/From 拆分**：长连线自动拆分
- **打包子系统**：选中模块快速打包
- **高亮未连接端口**：检测并高亮未连接端口
- **工具栏集成**：通过 sl_customization.m 集成到 Simulink

---

## 版本规划

### [3.1.0] - 已交付架构重构（原 3.0.0 规划，版本号因 3.0.0 已用于"更名发布"顺延）

> 总体目标：架构现代化、功能体系化、体验专业化。经可行性评估（2026-09-08），条目按优先级分批交付，部分原条目已调整或移除（见文末调整记录）。

#### 第一批（原 3.0.0-alpha，已于 3.3.0 交付；"连线样式着色"经评估移除）

- [x] 统一日志系统：分级日志（DEBUG/INFO/WARN/ERROR），GUI 状态栏与命令行双输出
- [x] 自动化测试体系：MATLAB Unit Test 回归测试（临时模型夹具），接入一键部署脚本
- [x] 操作进度条：大批量操作显示进度与取消按钮（uiprogressdlg；仅 GUI 路径弹出）
- [x] 主窗口可缩放布局：uigridlayout 自适应，记住窗口位置与尺寸
- [x] 用户级配置文件：JSON/mat 覆盖默认配置，带校验与说明（定稿：嵌套 JSON）
- [x] 设置界面：GUI 内图形化编辑配置（高价值子集，写入用户 JSON）
- [x] 操作结果反馈面板：批量操作成功/跳过明细，失败对象一键定位
- [x] 连线样式着色：~~信号线颜色/线型批量设置与清除~~（3.3.0 评估后移除，理由见该版"调整记录"）

#### 1. 重新设计项目架构，优化性能与工程质量（已于 3.1.0 交付）

- [x] 大规模模型性能优化：批量 set_param 与缓存扫描，目标较当前约 3~5 倍提速（高亮等依赖 Simulink 逐块内部机制的功能除外）——实测：端口对齐 >50×、大小统一 ~3.3×、对齐 ~1.45×（写侧受 Simulink 逐块连线重排约束）、命名 ~1.2×（写密集）
- [x] 函数命名空间化：迁移至 +simutidy 包，保留 sl_* 兼容包装，Toolstrip 与菜单命令同步迁移
- [x] 兼容层抽象：R2022b+（Toolstrip）与旧版（经典菜单）双轨支持，特性检测集中管理

#### 2. 功能拓展，新增能力

- [ ] 跨子系统批量操作：对齐/命名/端口对齐支持跨层多选（拆分 Goto/From 受语义限制除外）
- [ ] 模板管理：常用子系统/接口布局保存为模板并一键套用
- [ ] 代码生成配置检查：检查配置项并生成报告（部分检查项需 Simulink Coder 许可）
- [ ] 模型对比：集成官方 visdiff 一键调用
- [ ] 操作历史列表：记录 SK2 操作历史（回退依赖 Simulink 原生 Ctrl+Z）
- [ ] 国际化：中英双语（推迟至 3.x 后期）

#### 3. UI 界面美化，提升使用体验（已于 3.2.0 交付）

- [x] 深色主题：GUI 部分优先（3.2.0 交付：双调色板 + 切换按钮 + 偏好持久化）
- [x] Toolstrip 图标深色变体：为深色背景生成亮色图标（替代原 48px 图标方案）——3.2.0 最小交付（icons_dark/ 生成能力；自动切换视需求另行评估）
- [x] 新手引导：首次启动功能导览（已提前至 2.7.1 交付）

#### 可行性评估调整记录（2026-09-08）

- [x] 移除"键盘快捷键系统"：Simulink 无公开快捷键编程 API，替换为 SK2 命令面板
- [x] 移除"48px 图标精修"：Toolstrip 控件仅消费 16/24px，无使用场景
- [x] 移除"自研模型对比"：直接集成官方 visdiff
- [x] 性能目标修正：由"1 万块秒级"调整为"约 3~5 倍提速"
- [x] 撤销深度集成/重做面板降级：Simulink 无撤销锚点公开 API，原生 Ctrl+Z 已覆盖逐步撤销
- [x] SK2 命令面板：关键字过滤并执行功能（替代原快捷键方案）,意义不大，不做

## 版本说明

- **主版本号 (Major)**：不兼容的 API 变更
- **次版本号 (Minor)**：向后兼容的功能新增
- **修订号 (Patch)**：向后兼容的问题修复
